// meta-model-entity.ts — DataType marker consumed by the presentation system
// (presentation-loader.ts, meta-model-presentation-source.ts,
// presentation-scaffold.ts, compiled-presentation.ts). The class is passed as a
// context symbol ({MetaModelEntity}) into the compiled-presentation instantiator
// so generated resource dictionaries can declare `DataType = MetaModelEntity`.
// The drawer stack that previously populated its properties has been removed.
export class MetaModelEntity {}
